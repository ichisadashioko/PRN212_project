<Window x:Class="PRN212_project.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PRN212_project"
        mc:Ignorable="d"
        Title="Quản lý cư dân" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundColor}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="HỆ THỐNG QUẢN LÝ CƯ DÂN"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,30"
                   HorizontalAlignment="Center"
                   Foreground="{StaticResource PrimaryColor}"/>

        <!-- Main Content -->
        <Border Grid.Row="1" 
                Background="White"
                CornerRadius="8"
                Padding="30">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.3"/>
            </Border.Effect>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Resident Management Button -->
                <Button Grid.Column="0" Grid.Row="0"
                        Click="btn_2_1_window_Click" 
                        Name="btn_2_1_window" 
                        Height="120"
                        Margin="10">
                    <StackPanel>
                        <TextBlock Text="👥" FontSize="32" HorizontalAlignment="Center"/>
                        <TextBlock Text="Quản lý thông tin cư dân"
                                 FontSize="16"
                                 TextWrapping="Wrap"
                                 TextAlignment="Center"
                                 Margin="0,10,0,0"/>
                    </StackPanel>
                </Button>

                <!-- Registrations Button -->
                <Button Grid.Column="1" Grid.Row="0"
                        Name="RegistrationsWindow1_btn" 
                        Click="RegistrationsWindow1_btn_Click"
                        Height="120"
                        Margin="10">
                    <StackPanel>
                        <TextBlock Text="📝" FontSize="32" HorizontalAlignment="Center"/>
                        <TextBlock Text="Quản lý đăng ký"
                                 FontSize="16"
                                 TextWrapping="Wrap"
                                 TextAlignment="Center"
                                 Margin="0,10,0,0"/>
                    </StackPanel>
                </Button>

                <!-- Login Button -->
                <Button Grid.Column="0" Grid.Row="1"
                        Name="login_window" 
                        Click="login_window_Click"
                        Height="120"
                        Margin="10">
                    <StackPanel>
                        <TextBlock Text="🔐" FontSize="32" HorizontalAlignment="Center"/>
                        <TextBlock Text="Đăng nhập"
                                 FontSize="16"
                                 TextWrapping="Wrap"
                                 TextAlignment="Center"
                                 Margin="0,10,0,0"/>
                    </StackPanel>
                </Button>

                <!-- Logs Button -->
                <Button Grid.Column="1" Grid.Row="1"
                        Name="all_logs_window" 
                        Click="all_logs_window_Click"
                        Height="120"
                        Margin="10">
                    <StackPanel>
                        <TextBlock Text="📊" FontSize="32" HorizontalAlignment="Center"/>
                        <TextBlock Text="Xem nhật ký"
                                 FontSize="16"
                                 TextWrapping="Wrap"
                                 TextAlignment="Center"
                                 Margin="0,10,0,0"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>
